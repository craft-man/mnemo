---
description: Load minimal mnemo startup context manually
argument-hint: "[--code]"
---

Run the mnemo context skill to load only the compact session brief, optional global user profile, and optional codebase graph report.
